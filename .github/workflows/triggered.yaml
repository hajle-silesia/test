name: Triggered

on:
  repository_dispatch:
    types:
      - test
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    if: github.event_name == 'repository_dispatch' && github.event.client-payload == 'true'

    steps:
      - uses: actions/checkout@v4

      - name: Step from triggered workflow
        run: |
          echo "Step from triggered workflow"
